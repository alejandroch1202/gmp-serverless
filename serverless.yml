service: gmp

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  region: eu-central-1

plugins:
  - serverless-offline

functions:
  totalSupply:
    handler: src/functions/handler.totalSupply
